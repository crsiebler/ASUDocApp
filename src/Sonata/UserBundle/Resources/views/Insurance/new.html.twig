{% extends '::base.html.twig' %}

{% block headline %}Add Insurance Information{% endblock headline %}

{% block content_content %}
{{ parent() }}
<form action="{{ path('insurance_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('insurance') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
