{% extends '::base.html.twig' %}

{% block headline %}Add Primary Doctor{% endblock headline %}

{% block content_content %}
{{ parent() }}
<form action="{{ path('new_primary_doctor', { 'userID': userID, 'userName': userName }) }}" method="post" >
    <input type="text" name="name" />
    <p>
        <button type="submit">Submit</button>
    </p>
</form>

<ul class="record_actions">
    {% if is_granted('ROLE_USER') %}
        <li>
            <a href="{{ path('user_splash') }}">View Splash Page</a>
        </li>
    {% endif %}
</ul>
{% endblock content_content %}

{% block content_sidebar %}{% endblock content_sidebar %}