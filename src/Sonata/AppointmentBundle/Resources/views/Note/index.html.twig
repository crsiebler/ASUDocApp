{% extends '::base.html.twig' %}

{% block headline %}View All Notes{% endblock headline %}

{% block content_content %}
{{ parent() }}
<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Note</th>
            <th>Datecreated</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('note_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.note }}</td>
            <td>{% if entity.dateCreated %}{{ entity.dateCreated|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
            <ul>
                <li>
                    <a href="{{ path('note_show', { 'id': entity.id }) }}">show</a>
                </li>
                <li>
                    <a href="{{ path('note_edit', { 'id': entity.id }) }}">edit</a>
                </li>
            </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('note_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock content_content %}

{% block content_sidebar %}{% endblock content_sidebar %}